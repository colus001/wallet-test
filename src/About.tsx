import React from 'react';

const About: React.FC = () => (
  <div>
    <h1>About</h1>
    <p>
      This application demonstrates React, TypeScript, and modern web
      development practices.
    </p>
  </div>
);

export default About;
